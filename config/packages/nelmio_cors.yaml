nelmio_cors:
    defaults:
        origin_regex: true
        allow_origin: ['*']
        allow_methods: ['GET', 'OPTIONS', 'POST', 'DELETE']
        max_age: 3600
    paths:
        '^/api/registration/state/{registrationProcessId}': ~